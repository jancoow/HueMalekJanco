<Page
    x:Class="HueLamp.SettingsLamp"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HueLamp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <RelativePanel Grid.Row="0">
            <TextBlock RelativePanel.AlignHorizontalCenterWithPanel="True" FontSize="24" Margin="0,0,0,0" Text="{Binding Name}" />
        </RelativePanel>

        <StackPanel Grid.Row="1">
            <TextBlock FontSize="24">Colors</TextBlock>
            <Slider x:ConnectionId='4' Maximum="100" Value="{Binding HueColor, Mode=TwoWay}"                                                                                  />
        </StackPanel>

        <StackPanel Grid.Row="2">
            <TextBlock FontSize="24">Brightnes</TextBlock>
            <Slider x:ConnectionId='3' Maximum="100" Value="{Binding Brightnes, Mode=TwoWay}"                                                                                  />
        </StackPanel>

        <StackPanel Grid.Row="3">
            <TextBlock FontSize="24">Saturation</TextBlock>
            <Slider x:ConnectionId='2' Maximum="100" Value="{Binding Brightnes, Mode=TwoWay}"                                                                                  />
        </StackPanel>

        <StackPanel Grid.Row="4">
            <TextBlock FontSize="24">
            	<Run Text="Switch-ON-OFF"/>
            </TextBlock>
            <ToggleSwitch x:ConnectionId='1' Margin="20,10,0,10"                               >
                <ToggleSwitch.OffContent>Off</ToggleSwitch.OffContent>
                <ToggleSwitch.OnContent>On</ToggleSwitch.OnContent>
            </ToggleSwitch>
        </StackPanel>
    </Grid>
</Page>

